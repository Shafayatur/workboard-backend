# Direct dependencies only — versions chosen to work on Python 3.9 through 3.12.
# Transitive dependencies (requests, urllib3, asgiref, etc.) are left unpinned
# so pip can pick versions compatible with whichever Python runs this.
Django==4.2.30
djangorestframework==3.16.1
djangorestframework-simplejwt==5.5.1
django-cors-headers==4.9.0
django-cloudinary-storage==0.3.0
cloudinary==1.45.0
Pillow==11.3.0
python-dotenv==1.2.1
gunicorn==23.0.0
whitenoise==6.11.0
dj-database-url==3.0.1
psycopg2-binary==2.9.12